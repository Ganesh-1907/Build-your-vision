# Build Your Vision Frontend

This project is a frontend-only React application.

No backend service is required.

## Run locally

```bash
npm install
npm start
```

The development server will open at `http://localhost:3000`.
